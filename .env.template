# Copy this file to .env and fill the real values.
# Sensitive values must go into .env (which is gitignored).

# Base URL of the API under test
API_BASE_URL=https://thinking-tester-contact-list.herokuapp.com

# Optional: a dedicated test user (recommended for speed)
TEST_USER_EMAIL=your_test_user@example.com
TEST_USER_PASS=ChangeMe123!
